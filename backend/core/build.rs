// use std::fs;
// use std::path::Path;

// fn main() {
//     let wasm_path = Path::new("../target/wasm32-unknown-unknown/debug/test_plugin.wasm");
//     let out_dir = Path::new("target/debug");
//     fs::copy(wasm_path, out_dir.join("test_plugin.wasm"))
//         .expect("Failed to copy WASM file to output directory");
// }

fn main() {}
